# Local DB Notes for DocType Testing

For manual or integration testing against a real PostgreSQL instance instead of the default InMemory database, you can configure the connection string via environment variable or `appsettings.Development.json`.

Example local database URL (provided by user):
